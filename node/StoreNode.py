import secrets
"""
StoreNode is a separate node to store exact information
"""

class StoreNode():
	"""
	initial the store node
	@owner : String  owner of the storeNode
	@inform : exact information (in our case, it is address)
	"""
	def __init__(self, owner, inform ):
  		self.owner=owner
      self.inform=inform

  def getOwner(key):
    if secrets.compare_digest(key)
    return self.owner

  def getInfo():
    return self.information

  def update(newI):
    self.inform=newI



